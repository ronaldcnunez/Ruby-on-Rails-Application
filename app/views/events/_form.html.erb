<%= form_for @event do |f| %>
  <%= f.label :date %>
  <%= f.text_field :date %>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <%= f.collection_select(:artist_id, Artist.all, :id, :name) %>
  <%= hidden_field_tag 'event[venue_id]', @venue.id %>
  <%= f.submit %>
<% end %>
