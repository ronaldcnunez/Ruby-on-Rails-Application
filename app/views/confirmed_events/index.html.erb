<h1>Granudo Events</h1>
  <% if logged_in? && current_user.class == Venue %>
    <h2> <%= link_to "Create a new Event", new_event_path %> </h2>
    <br> <br>
  <% end %>

<% @confirmed_events.map do |confirmed| %>
<h4>
    <%=link_to "#{confirmed.artist.name}", artist_path(confirmed.artist.id)%>
    <%= " @ " %>
    <%= link_to "#{confirmed.venue.name}", venue_path(confirmed.venue.id) %>
    <%= "(#{confirmed.date})" %><br>
    <p><%= link_to "#{confirmed.description}", confirmed_event_path(confirmed) %>
    </p>
      <br>
</h4>
<h5>
  <br>
  <br>
  <% end %>
  <br>
  <br>
